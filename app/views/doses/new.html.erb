<%= simple_form_for @dose do |f| %>
  <%= f.input :description %>
  <%= f.input :cocktail_id %>
  <%= f.input :ingredient_id %>
  <%= f.button :submit %>
<!--   <%= f.button :submit 'NEW DOSE', :class => 'btn btn_primary' %> -->
<% end %>
